<h1> HB RESTAURANT MENU</h1>
  <div class="btn-group">
  <% @sections.each do |section| %>
    <%= link_to section.name, menu_path(section_name: section.name), class: "btn #{params[:section_id] == section.name ? 'btn-primary' : 'btn-secondary' }"%>
  <% end %>
  </div>